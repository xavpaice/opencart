apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: opencart
  name: opencart
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    io.kompose.service: opencart