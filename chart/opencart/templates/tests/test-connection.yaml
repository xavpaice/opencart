apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opencart.fullname" . }}-test-connection"
  labels:
    {{- include "opencart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opencart.fullname" . }}:80']
  restartPolicy: Never
